import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    width: "100%",
    alignSelf: "center",
  },

  pinIcon: {
    marginTop: 50,
    alignItems: "center",
  },

  submitButton: {
    marginTop: 80,
  },
});

export default styles;
