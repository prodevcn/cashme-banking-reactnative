// TODO: Implement storage to work with token
export const setToken = data => {
};

export const getToken = () => {
}

export const removeToken = () => {
};

export const isAuthenticated = () => !!getToken();
