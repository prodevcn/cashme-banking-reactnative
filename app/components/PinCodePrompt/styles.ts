import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  modal: {
    flex: 1,
    backgroundColor: "#438aed",
  },
  container: {
    flex: 1,
    alignItems: "center",
  },
});

export default styles;
