import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  icon: {
    display: "flex",
    flexDirection: "row",
  },

  flower: {
    marginRight: 10,
  },
});

export default styles;
