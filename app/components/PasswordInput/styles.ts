import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  passwordSection: {
    marginTop: 20,
    position: "relative",
  },
  eyeIcon: {
    position: "absolute",
    top: 25,
    alignSelf: "flex-end",
  },
});

export default styles;
