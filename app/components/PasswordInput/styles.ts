import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  passwordSection: {
    marginTop: 20,
    position: "relative",
  },
  eyeIcon: {
    position: "absolute",
    top: 20,
    alignSelf: "flex-end",
  },
  inputLabel: {
    color: "black",
  },
});

export default styles;
